---
name: Release

on:
  push:
    tags:
    - '*'

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup registry url with NPM
      uses: actions/setup-node@v2
      with:
        registry-url: https://registry.npmjs.org
        node-version: 16

    - name: Install dependencies
      run: yarn install --frozen-lockfile

    - name: Publish release @latest
      run: yarn release --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      if: ${{!contains(github.ref, '-alpha')}}

    - name: Publish release @next
      run: yarn release --access public --tag next
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      if: ${{contains(github.ref, '-alpha')}}

    - name: Rename before Publish to GitHub Packages
      run: 'sed -i "s/\"name\": \"/\"name\": \"@mogeko\//g" package.json'

    - name: Setup registry url with GitHub Packages
      uses: actions/setup-node@v2
      with:
        registry-url: https://npm.pkg.github.com
        node-version: 16
        scope: '@mogeko'

    - name: Publish release @latest
      run: yarn release
      env:
        NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{!contains(github.ref, '-alpha')}}

    - name: Publish release @next
      run: yarn release --tag next
      env:
        NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      if: ${{contains(github.ref, '-alpha')}}
...